// Common types used across components

export interface PhotoItem {
  imageUrl: string;
  title: string;
  date: string;
}

export interface VideoItem {
  imageUrl: string;
  title: string;
  duration: string;
}
